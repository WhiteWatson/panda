import { Image, View } from "@tarojs/components";
import { AtButton, AtInput, AtSelect, AtRange } from "taro-ui";

export default function Index(props) {
  const {
    name,
    type,
    value,
    placeholder,
    title,
    maxLength,
    disabled,
    required,
  } = props;

  return;
}
